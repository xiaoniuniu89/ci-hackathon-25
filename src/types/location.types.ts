export interface UserLocation {
  countryCode: string
  countryName: string
  city: string
  currency: string
  currencySymbol: string
}

export interface CurrencyInfo {
  code: string
  symbol: string
  rate: number
  name: string
}
